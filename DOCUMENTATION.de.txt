R4OS Recovery: Buildgrundlage und Uebernahmebestand
=================================================

Stand 0.76.23
------------

Das Monorepo besitzt einen festgehaltenen Quell-/Binaer-Ausgangsstand und
einen eigenen Recovery-Kernel. recovery.elf startet mit Limine unter BIOS
und x64-UEFI ohne SYSTEM-Partition. Seit 0.76.3 laedt Limine ein vollstaendiges
FAT32-Laufzeitvolume in den RAM; C: und das regulaere Terminal arbeiten daraus.
Seit 0.76.4 erkennt Recovery physische Medien und ordnet sie anhand der
tatsaechlichen Limine-Startdatei zu. Seit 0.76.6 besitzt RECOVERY.R4X das Menue
und den eingebetteten Konsolenhost. Netzwerk und Fernzugriff starten seit
0.76.7 aus der RAM-Laufzeit.

Pruefen: Build.bat beziehungsweise ./Build.sh, optional -Mode Verify.
Beide duennen Starter rufen dieselbe Build.ps1 mit PowerShell 7 auf.
Der Aufruf funktioniert aus jedem Arbeitsverzeichnis und liest nur lokale
Recovery-Eingaben. Ergebnis: Artifacts/Verification/inventory.json.
Es werden weder normale Workspace-Module neu gebaut noch Eingaben aus dem
Workspace kopiert oder ueber das Netz nachgeladen.

-Mode Kernel baut das zugehoerige Artifacts/Runtime/runtime.img und
Artifacts/Kernel/bin/recovery.elf mit dem festgehaltenen
Zig, lokalem Contract und ReleaseSafe/soft_float. -Zig waehlt bei Bedarf das
Hostwerkzeug; der Default kommt aus dem benachbarten DevKit. Kernel/Build.sh
und Kernel/Build.bat delegieren denselben Einstieg. Kernel/recovery_main.zig
ist die separate Bootfolge, Kernel/main.zig bleibt als Herkunftsreferenz.
Recovery VERSION.R4S und Kernel/VERSION.R4S besitzen eigene Versionen.

Die Grundfolge uebernimmt CPU, Speicher, Interrupts, Timer, SMP4-Scheduling,
Programmlebenszyklus, Service-IPC und DriverWork aus den festgehaltenen
Besitzern. Sie startet keinen Desktop und sucht keine SYSTEM-Partition.
Limine-Modulbereiche muessen vollstaendig als kernel_and_modules reserviert
sein; sie werden nicht als freie PMM-Seiten aufgenommen. Controllergebundene
Blockworker starten nach dem Einbinden des RAM-Blockgeraets. Das regulaere
Artefakt startet RECOVERY.R4X als konfigurierte Shell. Der Kernel bindet deren
BootReady-Meldung an die gestartete Instanz und ueberwacht den Shellbestand.
Ein unerwartetes Ende erreicht den regulaeren Fatalpfad. Die Anwendung
uebernimmt den Framebuffer; Hintergrundlogs bleiben seriell und im Bootlog.
Das vorhandene TERMINAL.R4X laeuft als eigener gehosteter Konsolenprozess.

Bootnachweis
------------

-Mode BootTest baut einen eigenen Diagnosekernel und fuehrt kurze BIOS- und
UEFI-Laeufe mit vier vCPUs aus. -Firmware Bios beziehungsweise Uefi begrenzt
die Auswahl. -BootProbe reboot prueft zusaetzlich den regulaeren Resetpfad;
Standard ist Herunterfahren. Der Diagnosecode fehlt im regulaeren Artefakt.

Tools/Test-Boot.ps1 erstellt eine technische 128-MB-Fixture mit genau einer
FAT32-Partition und ohne SYSTEM/NTFS. Sie ist nicht das geplante Releaseimage.
Der ImageCreator kommt aus der ausdruecklich aktualisierten lokalen
Platform/Distribution-Kopie, die QEMU-Hostauswahl weiterhin aus dem
urspruenglichen Distribution-Quellenarchiv. Neben recovery.elf laedt Limine einen bekannten
4096-Byte-Pruefblock. Vier an CPU 0 bis 3 gebundene Worker allozieren, warten,
pruefen und geben Speicher frei. Vorher und danach bleibt der Pruefblock
unveraendert. Der Test verlangt den vollstaendigen SMP4-Erfolgsmarker und
regulaeren QEMU-Abschluss; Crash und Zeitlimit schlagen fehl.

Ergebnisse und serielle Logs liegen unter Artifacts/BootProbe/poweroff
beziehungsweise reboot. Linux benoetigt ein passendes OVMF-Code-/Variablen-
paar; DevKit-Setup installiert es mit. Windows verwendet die QEMU-Firmware.
Der Teststarter erlaubt explizite -Qemu, -LimineRoot, -OvmfCode und -OvmfVars.
Die vorliegende Abnahme lief auf Linux mit KVM: BIOS/UEFI, Poweroff/Reset,
vier vCPUs und 1024 MB RAM. Eine Windows- oder Hardwareabnahme wird damit
nicht behauptet. Die RAM-Menge ist noch keine finale Kapazitaetszusage.


Vollstaendige RAM-Laufzeit
-------------------------

-Mode Runtime erzeugt Artifacts/Runtime/runtime.img: ein 64-MB-FAT32-Volume
mit Bootsektor bei null, beiden FATs, Verzeichnissen und saemtlichen Runtime-
Dateien. Der explizit uebernommene kanonische ImageCreator verwendet dafuer
--volume-only. Normale MBR-Images und reine Volumes teilen denselben Code.
Die Aenderung beim Besitzer behebt zugleich ueberlappende mehrclusterige
Hauptverzeichnisse, fehlende Kapazitaetsgrenzen und die nur im BPB vorhandene
Datentraegerbezeichnung. Herkunft: Provenance/owner-update-0.76.3.json und
Legal/Changes/Distribution-0.76.3.patch auf dem genannten Basiscommit.

Der Build bindet Groesse und SHA-256 des gesamten Images in recovery.elf.
Limine muss genau ein Laufzeitmodul mit module_string recovery.runtime=1
laden. Fehlende, doppelte, verkuerzte oder byteveraenderte Module werden vor
der Einbindung von C: mit einem konkreten RECOVERYRAM-Fehler abgewiesen.
Die Root-Recovery-Version, Runtime-Identitaet und Terminal-Versionsdatei
muessen beim Build uebereinstimmen. recovery.elf und runtime.img bilden damit
ein festes Paar, das spaeter gemeinsam veroeffentlicht und ersetzt wird.

Das bereits von Limine geladene Image bleibt in seinen reservierten
Speicherbereichen. Der fruehe RAM-Blockadapter verwendet begrenzte synchrone
Lese-/Schreiboperationen; die vorhandenen Blockworker, Cache-, FAT32-, VFS-
und Dateianfragen bleiben der produktive I/O-Weg. C: wird vor jeglicher
physischer SYSTEM-Suche fest als FAT32 mit Rolle ram gebunden. Es ist
beschreibbar; Konfiguration, R4STD, Module, Fonts, Media und C:\TEMP sind
vollstaendig im RAM. Aenderungen gehen beim Neustart verloren. R: gehoert zur bestaetigten physischen Bootquelle; sein Ausfall beendet
die RAM-Laufzeit nicht.

Zwei unveraenderte Terminalfonts stammen aus dem Besitzerbuild. Das vom
Benutzer gelieferte Hintergrundbild liegt bytegleich unter
C:\R4OS\MEDIA\RECOVERY.BMP; RECOVERY.R4X stellt es seit 0.76.6 dar.
Die regulaeren R4M0-Lader, R4XStart-Tabellen und R4L-Bindungen sind uebernommen.
Die Hintergrunddienste werden erst nach ihrer Netzwerkgrundlage automatisch
gestartet; SERVMAN und die Dienstmodule liegen bereits im Laufzeitvolume.

-Mode RuntimeTest verwendet denselben begrenzten SMP4-Runner. Unter BIOS und
UEFI bootet die technische Fixture von USB. Nach der C:-Einbindung entfernt
der Host per QMP den USB-Datentraeger und beide Blockbackends, prueft ihre
Abwesenheit und setzt erst dann die Gastabnahme fort. Saemtliche Userland-
Aufnahme erfolgt danach: R4STD, R4D-Erkennung und -Aktivierung, bedarfsgeladenes
JSON.R4P, Terminalfonts und vollstaendige BMP-Dateilesung mit Hashvergleich.
Das unveraenderte Terminal startet HELP und SERVMAN, liest Konfiguration,
schreibt/liest/loescht C:\TEMP\LATE.TXT und benutzt die R4STD-Datums-API.
Rueckkehrcodes, vererbte Konsolenausgabe und Programmbereinigung werden
geprueft. Vier weitere kurze Faelle belegen die vorgenannten Modulfehler.
Logs, Kernel-/Imagehashes und QMP-Nachweis liegen in Artifacts/BootProbe/ram.
Nach -Mode Kernel prueft Tools/Test-Boot.ps1 -Action terminal ausserdem den
regulaeren interaktiven Shellstart: nach der Bereitschaft USB entfernen,
Terminal im Menue waehlen, HELP und POWEROFF ueber die Tastatur eingeben und
den echten Kernel-Poweroff mit QEMU-Abschluss verlangen. RECOVERY.R4X nutzt
Hostkind none und liest damit die physische Tastatur. Das untergeordnete
Terminal nutzt terminal_mode und erhaelt Eingaben vom Recovery-Consolehost.

Die 64-MB-Laufzeitgroesse und 1024 MB Gast-RAM sind vorlaeufige Build-/
Testgroessen. Der verbindliche reale Kapazitaetsnachweis bleibt 0.76.23.

Uebernahme
----------

Provenance/import-plan.json benennt 41 explizite Quellcommits, die verwendete
Zig-Version und 37 regulaere R4M0-Module. Tools/Import.ps1 ist der getrennte,
ausdrueckliche Einmalimport. Er prueft Revisionen und Build-Eingaben, baut
ueber die Besitzerstarter, prueft Container mit dem kanonischen R4XBuilder,
liest ihre tatsaechlichen Imports/Exports und archiviert die Originalquellen.
Dokumentationskorrekturen im Workspace koennen vorhanden sein; Archive
enthalten trotzdem exakt HEAD, und veraenderte Bauquellen werden abgewiesen.
Ein teilweise fehlgeschlagener Import darf nur bytegleiche Quellkopien
wiederverwenden und ueberschreibt keine bearbeitete Recovery-Kernelkopie.

Kernel/, Platform/Contract/ und Platform/SDK/ sind lokale Quellkopien.
Legal/Sources/ enthaelt die zugehoerigen Originalquellen als Git-Archive,
Legal/Notices/ die mitgebrachten Lizenzhinweise, Legal/Manifests/ die
originalen Modulmanifeste. Herkunft, Commits, SHA-256, Modulversionen,
Imports, Exports, Ressourcen und Protokollrollen stehen in
Provenance/inputs.lock.json. Keine automatischen Updateverweise im Build.
Geerbte Standalone-URLs in den eingefrorenen Zig-Paketmetadaten sind nur
Herkunft; Recovery-Builds muessen ihre eigenen lokalen Forks benutzen.

Die Pruefung vergleicht alle Dateien in Kernel, Platform, Runtime und Legal
mit dem festgehaltenen Bestand und lehnt auch unbekannte Eingaben ab.
Sie prueft binaere Import-/Exporttabellen, manifeste Modulidentitaet,
Protokollabhaengigkeiten, Quellenarchive und den eingebetteten Contract.
Der Bytevergleich funktioniert hostuebergreifend ohne Git-Zeilenendumbau.
Eine bewusste lokale Weiterentwicklung wird mit Begruendung im Lock
fortgeschrieben; Originalhash und unveraenderliches Quellenarchiv bleiben
als Herkunft erhalten. Der Build selbst schreibt diese Hashes niemals neu.

Laufzeitbedarf und Grenzen
-------------------------

Storage: AHCI, ATAPIO, NVME, USBMSC und XHCI. XHCI.R4D aktiviert den Host; seine
Controllerimplementierung und USB-MSC-I/O leben weiterhin im Kernel.
USB: USBHID, HIDREPORT, USBBOT und USBSCSI. Die gemeinsame Keyboardqueue
und PS/2-Eingabe bleiben Kernelgrundlagen. Es wird keine Maus initialisiert.
Netz: VIRTNET, RTL8139 und RTL8168; NETETH, NETARP, NETIPV4, NETICMP,
NETUDP, NETTCP, NETDNS und NETDHCP sowie R4HTTP, R4TLS und JSON.
Dienste: DHCPSVC, DNSSVC, TCPSVC, UDPSVC, SSHD und FTPSVC.
Konsole: TERMINAL, SERVMAN, HELP, IPCONFIG, SYSINFO, HTTPGET und NETCAT.
Runtime-Library: R4STD mit DATE_V1/TIME_V1 fuer TERMINAL.
Zertifikate, Tastatur-/Zeitkonfiguration und eingebettete Modulressourcen
werden ebenfalls festgehalten. Weitere Recovery-eigene Laufzeitdateien
kommen mit ihren jeweiligen Implementierungsschritten hinzu.

Die 37 Binaerdateien haben 53 explizite Imports. Die benoetigten Plattform-
Provider sind R4SYS, R4DESK, R4DEV und R4NET jeweils Query:1. Der vollstaendige
eingefrorene Contract bleibt erhalten; seine Tabellen werden nicht aufgrund
eines Importnamens verkleinert. Ein Query-Import beschreibt keine exakte
Liste ausgefuehrter Slots. Fuer die Reduktion gelten folgende Verbraucher:

R4SYS: Console/STDIO, Datei- und Ressourcen-I/O, Allocator/VM, Zeit,
Umgebung/Registry, Programm-/Threadhandles, Services und Endpoints/Waits.
R4DESK: TERMINAL/SSHD/NETCAT benutzen ueber die SDK-Fassaden Programmstart,
Consolehost, Eingabe, Metriken, State, Revision und Ausgabetranskript.
SSHD benoetigt zudem unabhaengige Consoleinstanzen und Workerthreads.
R4DEV: Treiber-/Protokollregistrierung, Block-/Geraetegrundlagen,
USB-Dispatcher, Kernel-/Inventarausgabe und Diagnosefunktionen.
R4NET: NIC/TCP/UDP/DNS/DHCP, Socket-/Servicepfade und Protokolldispatch;
HTTP/TLS bleiben bei ihren existierenden SDK-/Protokollbesitzern.

R4DESK als Plattformname ist kein Auftrag, R4DESK.R4X zu installieren.
Es sind kein Desktopprogramm, keine GUI-App, keine Audio-/Window-/RDP-
Dienste und keine Emulator-/Browsermodule im Runtime-Bestand enthalten.
Die Quellkopien behalten zunaechst auch unbenutzte SDK-/Kerneldateien, um
den Ausgangsstand unveraendert nachvollziehen zu koennen. Welche Kernelteile
tatsaechlich im getrennten Artefakt benoetigt werden, wird beim Build und
der Gastabnahme nachgewiesen, nicht durch eine reine Textsuche behauptet.

Die Bestandspruefung belegt statische Kompatibilitaet und Integritaet.
Der RAM-Gastnachweis prueft die benoetigten Lader und repraesentative
unveraenderte Module. Damit sind noch keine echte Datentraeger-, Netzwerk-
oder SSH-/FTP-Abnahme und keine vollstaendige Treiber-Hardwarematrix belegt;
diese erhalten ihre folgenden begrenzten SMP4-Nachweise.

Vertraege und Platzbedarf
------------------------

RecoveryTools/Contracts.de.txt spezifiziert Bootquellenidentitaet,
BOOT-Installationsbeschreibung, GPT-Rollen, Release-/Kernelversionen,
Recovery-Manifest, state.r4s und die fachlichen Besitzergrenzen.
Provenance/capacity-estimate.json haelt die erste gemessene Bestandsgroesse,
repraesentative vorhandene R4OS-Releases und ausdrueckliche Annahmen fest.
Die 512-MB-RECOVERY-Groesse bleibt vorlaeufig. Die verbindliche Abnahme mit
beiden echten Recovery-Staenden, altem/neuem Release-ZIP, FAT-Verwaltung und
Arbeitsspeicher folgt in 0.76.23. Keine Groessenaenderung allein aus einer
Projektion und keine Nutzung von DATA fuer Quellen-/Arbeitsablage.

Storage und Bootquelle (0.76.4)
------------------------------

Die festgehaltenen R4D-Module XHCI, AHCI, NVME, ATAPIO und USBMSC werden
aus C: aktiviert. Der gemeinsame kernelresidente USB-MSC-Owner verwaltet
mehrere Geraete mit getrennten SCSI-/Endpoint-/Blockkontexten; der vorhandene
xHCI-Owner serialisiert die gemeinsame Transportgrundlage. Die normalen
Treiberbinaerdateien bleiben unveraendert.

BootInfo kopiert den Limine-ExecutableFile-Eintrag einschliesslich GPT-IDs
und geladenem Pfad vor einer Speicherfreigabe. partition_table prueft MBR
mit begrenzter EBR-Kette beziehungsweise beide GPT-Header, beide identischen
Eintragsarrays, CRCs, Grenzen, Ueberlappungen und eindeutige Partition-IDs.
BOOT:/boot/r4os-installation.json wird begrenzt geparst und gegen diese
Tabelle geprueft. Doppelte JSON-Schluessel, fraktionale/ueberlaufende Zahlen,
Traversals, unbekannte Hauptschemata und widerspruechliche Rollen scheitern.

Die tatsaechlich geladene RECOVERY-Partition bestimmt CURRENT/PREVIOUS sowie
Installation und Bus. Geklonte Kennungen, fehlende Beschreibung oder falsche
Startpartition liefern keine automatische R:- oder destruktive Zielzuordnung.
Recovery bleibt dabei aus C: bedienbar. Beim lokalen oder ungeklaerten Start
wird USB-Speicher vor der physischen Mountaufnahme in der Blockschicht
ausgeschlossen; auch Geraeteinventar und spaetere Dateizugriffe beachten das.
USB-HID und der gemeinsame Hostcontroller bleiben davon unabhaengig.

R: bindet ausschliesslich die RECOVERY-Partition der bestaetigten Bootquelle.
Weitere geeignete Volumes erhalten freie Buchstaben ab D: unter Auslassung
von R:. Bestehende Zuordnungen werden nicht ersetzt; C: bleibt RAM und das
aktuelle Laufwerk. BIOSBOOT bleibt unlettered. Beim USB-Start ist der eigene
Stick von Install ausgeschlossen und fuer Update Recovery zulaessig. Der
eigene lokale Datentraeger bleibt ein gueltiges Install-Ziel. Die ausfuehrenden
Installer folgen in den dafuer geplanten Unterversionen.

Das Inventar bewahrt Kapazitaet, logische Sektorgroesse, Bus, logischen
Geraetenamen, GPT-/MBR-Kennung, Partitionen und Dateisystem-/Mountstatus.
Das Modellfeld ist optional: USB-SCSI liefert echte INQUIRY-Daten; der
bisherige R4D-Storage-Vertrag liefert fuer AHCI/NVME/ATAPIO keine Modelltexte.
Dort bleibt das Feld leer, statt den Registry-Namen als Hardwaremodell
auszugeben. Nicht erkannte/defekte Dateisysteme bleiben als Diagnose sichtbar.

Grenzen: 16 registrierte Blockgeraete einschliesslich RAM, 128 inventarisierte
Partitionen pro Medium, 26 FAT-/NTFS-/VFS-Zuordnungen je Besitzer (Buchstaben
begrenzen den Gesamtmountbestand). NTFS hatte zuvor nur vier Slots. FAT32
ueberschreibt bei Slotmangel keinen bestehenden Laufzeitzustand mehr. Die
Partitionstabelle verwendet u64-LBAs; die bisherigen FS-Basen sind u32, FAT32
verwendet weiterhin u32-Endadressen und NTFS hoechstens u32 Cluster. Fremde
Geometrien ausserhalb dieser Grenzen bleiben ungemountet; native 4Kn ist
nicht freigegeben. FAT32 prueft die Partitionsgrenzen vor dem Mount; der
NTFS-I/O-Adapter begrenzt jeden Zugriff auf sein ausgewaehltes Volume.

-Mode StorageTest baut einen Diagnosekernel und feste 2048-MB-GPT-Fixtures
mit fuenf Rollen. Tools/Storage-Fixtures.ps1 erzeugt ausschliesslich Dateien
in Artifacts; dies ist kein Produktionsinstaller. Der kurze SMP4-Gast prueft
USB- und lokalen AHCI-Start unter BIOS/UEFI, NVMe/PREVIOUS, geklonte IDs und
eine falsche Installationsbeschreibung. Zwei USB- plus AHCI-/NVMe-Medien,
sechs NTFS-Volumes, Datei-Zeugen jedes Mounts, ein unbekanntes MBR-Dateisystem,
C:/R:-Stabilitaet und die Zielregeln werden automatisch geprueft. Auf den
NTFS-Volumes bestaetigter Installationen werden Dateien erstellt, geflusht,
gelesen und geloescht; alle Gastmedien verwenden QEMU-Snapshots. Auch der
kurze SMP4-Starttest des normalen R4OS-Kernels ist erfolgreich.
Ergebnisse: Artifacts/BootProbe/storage/storage-results.json und Seriallogs.
Ausschliesslich Linux/KVM ist praktisch geprueft; kein Lenovo-Sichttest.

Tastatureingabe (0.76.5)
-----------------------

Recovery uebernimmt den internen PS/2-/i8042-/IRQ-Pfad sowie den gemeinsamen
Scancode-/Codepointpuffer und die englischen/deutschen R4OS-Layouts. Nach
der Medienaufnahme werden die festgehaltenen USBHID.R4P und HIDREPORT.R4P
aus RAM geladen und an den vorhandenen xHCI-/HID-Besitzer angeschlossen.
Protokollaufnahme erfolgt vor dem Controller-Owner, damit kein Modul-I/O
unter dessen Sperre laeuft. Der HID-Polltask und der xHCI-Topologietask
bleiben unabhaengig von der Sichtbarkeit der USB-Massenspeicher aktiv.

Die explizit uebernommene kanonische HID-Option bind_mouse=false gilt auch
nach Topologieaenderungen. Recovery initialisiert weder die PS/2-Maus noch
eine USB-Maus, keinen Mauszeiger und keinen Desktop. Normales R4OS behaelt
seine bisherigen HID-Standardoptionen. Die 37 Runtime-Binaermodule bleiben
bytegleich; ein zusaetzlicher USB-Tastaturtreiber ist dafuer nicht notwendig.

-Mode InputTest prueft SMP4-Gaeste fuer USB-/lokalen Start unter BIOS/UEFI
mit PS/2- beziehungsweise USB-Tastatur. Im USB-Tastaturfall ist i8042=off;
der Gast verlangt HID-Reportverarbeitung und unveraenderten PS/2-IRQ-Zaehler.
Eine zusaetzliche USB-Maus bleibt ungebunden. Der Host wartet positiv auf
Bereitschaftsmarker und sendet erst dann Hardware-Tastenereignisse per QMP.
Navigation, Enter, Escape, Backspace, Shift/Ctrl/AltGr, QWERTY/QWERTZ und
Unicode-Codepoints werden am gemeinsamen Eingabepfad geprueft. Danach
tippt der Host einen Befehl mit Korrekturtaste in die echte TERMINAL.R4X;
die resultierende Datei und das geordnete EXIT werden im Gast geprueft.
Dabei bleibt die normale ASCII-/Grossschreibungslogik des Terminals bestehen.
Sichtbare Menuezustaende prueft seit 0.76.6 die gesonderte UI-Abnahme;
reale Lenovo-/USB-Tastaturmodelle sind damit noch nicht abgenommen.

Menue und Consolehost (0.76.6)
-----------------------------

RecoveryTools/Menu besitzt RECOVERY.R4X als normale Konsolenanwendung.
module.R4MF bestimmt Version, Console-Klasse, Zielpfad und die vorhandenen
R4SYS-/R4DESK-/R4DRAW-/R4NET-Imports. Tools/RecoveryTools.ps1 baut sie gegen
den lokalen festgehaltenen SDK-/Contract-Stand und prueft den R4M0-Container
und seine Imports vor dem Imageeintrag. Artifacts/RecoveryTools/modules.json
beschreibt das eigene Artefakt. Die 37 uebernommenen Runtime-Module bleiben
unveraendert; runtime.img enthaelt zusaetzlich die eigene Anwendung. Auch
RecoveryTools-Quellen und Manifeste gehoeren zur SHA-256-Eingabepruefung.

Das originale 1672x941-BMP wird proportional eingepasst und zentriert.
Aus den Quellkoordinaten [391,223)-[1284,680) entsteht ein nach innen
gerundeter Monitorbereich. Nur dort werden Menue, Dialog, Fortschrittsbalken,
Status, Cursor und Konsolenzellen gezeichnet. Restflaechen bleiben schwarz;
der Rahmen wird nie zu Konsolenflaeche. Terminalfonts mit 8 oder 16 Pixeln
Hoehe kommen aus dem vorhandenen R4OS-Fontkatalog. Das Menue benoetigt
mindestens 280x160 Monitorpixel; kleinere Modi werden konkret abgewiesen.

Reihenfolge: Install R4OS, Update R4OS, Update Recovery, Manage Partitions,
Terminal, Exit and Restart. Pfeile wechseln mit Umlauf, Enter oeffnet den
Eintrag, Escape oder Enter schliesst einen Dialog. Rote Auswahl und helle
Schrift gelten fuer alle Modi. Seit 0.76.14 fuehren die ersten drei Eintraege
ueber Quelle, echte Ziele und eine Bestaetigung mit Back als Vorauswahl.
Paketverarbeitung und Schreibablaeufe sind seit 0.76.15 bis 0.76.20 verbunden.
Manage Partitions startet das seit 0.76.10 uebernommene R4PART.

Terminal startet die festgehaltene TERMINAL.R4X mit /NOAUTOEXEC und eigenem
Consolehost. Der Host liest begrenztes Transcript, Zustand und Revision,
meldet die verfuegbaren Zeilen/Spalten und leitet Tasteneingaben weiter.
Cursor, Farben, CLS und Scrollen gelten nur fuer diese Sitzung. Der
dauerhafte ProgramHandle besitzt Wait/Reap und fuehrt EXIT zur gleichen
Menueauswahl zurueck. Weitere Konsolen und Dienste behalten ihren Kontext.
Der Neustarteintrag steht nur ohne aktives Unterprogramm oder laufenden
Menueauftrag zur Verfuegung und nutzt den vorhandenen Flush-/Resetpfad.
Die gemeinsamen Regionsbelegungen sind seit 0.76.8 ueber den Storagevertrag verfuegbar.

SSH zeigt eine vom Netzkonfigurations-Snapshot gemeldete IPv4-Adresse erst
bei Link, Konfiguration und laufender SSHD an. Andernfalls erscheint der
Warte-/Startzustand. Es wird keine Beispieladresse als Gastadresse gezeigt.

-Mode UITest verwendet den echten Anwendungscode und einen Diagnosekernel,
der ausschliesslich das Argument /UISMOKE an die konfigurierte Shell reicht.
Der vorhandene SMOKE-Mirror macht deren stdout seriell beobachtbar. Eine
zweite unveraenderte Terminalinstanz mit terminal_window bleibt unsichtbar;
ihr vollstaendiges Transcript muss lokale Eingabe, CLS, Scrollen und EXIT
bytegleich ueberstehen und danach geordnet beendet und gereapt werden.
Die sichtbare Eingabe kommt ausschliesslich von der QEMU-USB-Tastatur bei
i8042=off. Der Test prueft sechs sichtbare Auswahlzeilen, Umlauf, Dialoge,
Fortschritt, Text/CLS/Scrollen, Rueckkehr und regulaeren Reset. P6-Screendumps
werden ausserhalb des Monitors gegen das Originalbild und alle Folgezustaende
pixelgenau verglichen. Gemeinsame PS7-Orchestrierung und ein kleines
frameworkfreies C#-Pruefmodul unterstuetzen beide Hosts. Dieser Nachweis ist
unabhaengig von der Gespraechs-Preview und verlangt keinen Benutzersichttest.

Der reale Bildstart deckte eine gemeinsame FAT32-Grenze auf: readFile
beendete den Lauf nach 4096 Clustern mit scheinbarem Teilerfolg. Bei den
512-Byte-Clustern des RAM-Volumes waren so nur 2 MB lesbar. Der kanonische
Kernel begrenzt nun durch Dateigroesse und Volume-Geometrie, prueft gueltige
Cluster und meldet ein vorzeitiges Kettenende als Fehler. Die ausdrueckliche
Uebernahme steht in Provenance/owner-update-0.76.6.json und der zugehoerigen
Legal-Patchdatei. Das originale 4720110-Byte-Bild wird danach vollstaendig
ueber die normale Datei-API geladen und in den Gast-Screendumps verifiziert.

Abnahme: Linux/KVM, SMP4, 1024 MB RAM. BIOS 800x600, 1024x768 und 1920x1080
sowie UEFI 1024x768 bestanden mit jeweils 16 Framebuffervergleichen, allen
Menueaktionen, Terminal- und Konsolenisolationspruefung. Windowsausfuehrung
und physische Hardware bleiben davon getrennt; der einzige Benutzersichttest
bleibt die abschliessende Lenovo-USB-Installation in 0.76.26.

0.76.7: Recovery-Netzwerk und Offline-Dateireparatur
-------------------------------------------------
recovery_network initialisiert NetCore, DHCP-Konfiguration, Service-IPC und
Netzwerkendpunkte. NETETH/ARP/IPV4/ICMP/UDP/TCP/DNS/DHCP werden ausserhalb
der NIC-Owner vorgeladen. VIRTNET, RTL8139 und RTL8168 werden gezielt versucht;
fehlende Hardware bleibt optional. RX und DHCP laufen asynchron. Ein eigener
Starttask fuehrt SERVMAN BOOT aus C: aus, ohne DHCP oder Listenerbereitschaft
zur Voraussetzung des lokalen Menues zu machen. SERVICES.R4S registriert
DNSSVC, DHCPSVC, TCPSVC, UDPSVC, FTPSVC und SSHD. Die verifizierten Dienst-
Defaults benutzen r4os / rosebud, SSH-Port 22, FTP-Port 21 und passiv 2020.

SSHD 0.1.4, FTPSVC 0.1.5 und TERMINAL 0.1.9 sind ausdruecklich aktualisierte
Binaerimporte; die uebrigen 34 Module bleiben festgehalten. Zugehoerige
Besitzerpatches, Grundcommits und neue Hashes stehen unter Legal/Changes und
Provenance/owner-update-0.76.7.json. Build importiert niemals automatisch.

SSH-Shell und Exec nutzen eigene Terminalkontexte. Interaktive lokale und
SSH-Sitzungen zeigen Recovery-Version, Bootmedium/Slot und Laufwerke.
SFTP/SCP/FTP benutzen dieselben Mounts, etwa /E/... fuer E:\... . Das Menue
leitet seine SSH-Zeile aus Dienststatus und der wirklich konfigurierten IP ab.
Ohne Netz, bei fehlendem Adapter und bei beschaedigtem SYSTEM bleibt die
RAM-Laufzeit bedienbar. BOOTMED.TXT wird aus der erkannten Startidentitaet
neu in C:\R4OS\CONFIG erzeugt; das physische Medium bestimmt keine Shell.

Offline-Reparatur erlaubt Lesen, Kopieren, Schreiben, Umbenennen und Loeschen
auch in BOOT/EFI auf einem physischen D:. C:-Systemdateien behalten die
normalen Transferregeln. Vorhandene Dateien lassen sich mit SSH-COPY aus
einem hochgeladenen RAM-Tempfile ersetzen; alternativ explizit loeschen und
neu hochladen. Transfers behalten ihre create-only Stage-/Claim-Publikation.
Es gibt keinen impliziten verlustfreien Mehrdatei-Updatevertrag; der regulare
SYSUPD-Weg bleibt im normalen R4OS. Programmstarts kommen nur von C: im RAM,
Module anderer Laufwerke bleiben als Reparaturdateien zugaenglich.

-Mode NetworkTest baut die Produktionslaufzeit und fuehrt begrenzte SMP4-
Faelle mit BIOS/VirtioNet, UEFI/RTL8139 sowie ohne NIC und mit beschaedigtem
SYSTEM aus. Host-OpenSSH prueft Shell, Exec, SFTP und SCP; ein unabhaengiger
Host-FTP-Client prueft aktive/passive Transfers. Binardaten-Hashes, direkte
Offline-Dateireparatur, RAM-Zielschutz, abgewiesene Offline-Programmstarts,
unveraenderter Menueframebuffer und regulaerer Reset gehoeren zu diesem
gebuendelten Nachweis. Hostclients und QEMU sind zeitlich begrenzt; Ergebnisse
stehen unter Artifacts/BootProbe/network/network-results.json. OpenSSH und
Zig-cc fuer den kleinen nativen ASKPASS-Helfer muessen vorhanden sein. Der
passive Gastport 2020 wird nur fuer diesen isolierten Runner lokal gebunden.
Der normale netzlose testquick bleibt unveraendert; kein manueller Sichttest
vor der vereinbarten Lenovo-Abnahme am Ende der Roadmap.

0.76.8: Gemeinsame Storage-API und exklusive Bearbeitung
------------------------------------------------------
Der explizit uebernommene Contract und SDK bieten generationiertes Inventar,
begrenztes Raw-I/O, Flush, Mount/Unmount, Rescan und Laufwerkszuordnung.
Kernel/access_state und operations koordinieren Geraete-/Volume-Generationen,
Dateinutzungen und exklusive ganze Datentraeger oder einzelne Partitionen.
Offene lokale Streams und SFTP/SCP/FTP-Nutzungen melden Belegt vor der
Aenderung. Neue betroffene Zugriffe werden waehrend des Claims abgewiesen;
unbeteiligte Laufwerke, Netzwerk und RAM-C: bleiben benutzbar.

Vor Raw-Zugriff wird betroffener Cache-Writeback abgeschlossen, Backend-Flush
bestaetigt und das Ziel ausgehaengt. Abschluss prueft Flush, liest die Tabelle
neu und bindet passende vorherige Identitaeten mit neuer Generation ein.
Fehlgeschlagene Ziele bleiben im Inventar. Programmende drainiert I/O und
uebergibt liegengebliebene Claims an den Cleanup-Worker. USB-BOT darf nach
Medienwechsel nicht auf der alten Geraeteregistrierung weiter schreiben.
Die fachliche GPT-/Formatierungslogik bleibt Aufgabe externer Werkzeuge.

SSHD 0.1.5 und FTPSVC 0.1.6 wurden beim Besitzer gebaut und ausdruecklich
uebernommen. SSH wertet terminale TCP-Pollantworten auch bei negativem
Rueckgabecode aus und gibt abgebrochene Sitzungen frei. FTP beruecksichtigt
das Laufwerkswurzelverzeichnis als gueltigen Elternpfad fuer Uploads.
Zig/C-Fassaden und ihre bestehenden Conformancefaelle sind im eingefrorenen
SDK enthalten. Legal/Changes und Provenance/owner-update-0.76.8.json halten
Quellpatches, Ausgangscommits, Artefakte und Hashes fest; die urspruenglichen
Quellarchive bleiben bestehen.

-Mode StorageAccessTest erweitert denselben PS7-Netzwerkpruefer mit echten
gehaltenen SFTP-Datei-/Verzeichnishandles, einem angehaltenen FTP-Upload,
parallelem exklusivem Zugriff, fremden Tokenbesitzern, Programmende und
Remountfehler samt Reparatur. Ein eigener 16-MB-NVMe-Testdatentraeger mit
QEMU blkdebug erzeugt nach erfolgreichem Raw-Schreiben einen echten
Flushfehler. Die Diagnose in RECOVERY.R4X verwendet ausschliesslich das
SDK und verweigert alle mutierenden Pfade ohne die feste technische
Fixture-GUID. Ergebnisdateien: Artifacts/BootProbe/storage-access/.

Abnahme: BIOS/VirtioNet 37.987 s, UEFI/RTL8139 35.450 s sowie netzlos mit
beschaedigtem SYSTEM 10.141 s, jeweils Linux/KVM SMP4 mit 1024 MB RAM.
Contract-Selbsttests, bestehende Storage-Hosttests und Zig/C-Conformance sowie
der normale SMP4-testquick bestanden. Keine Windows-/Lenovo-Ausfuehrung;
der vereinbarte einzelne Hardwaretest bleibt der finalen Unterversion
vorbehalten. Das Eingabeinventar enthaelt 858 Dateien und 37 importierte
Module; die Menuediagnose bleibt Teil des Recovery-eigenen Konsolenmoduls.

0.76.9: Gemeinsame Partitions- und Formatierungswerkzeuge
-------------------------------------------------------
Platform/SDK/r4os/storage_tools enthaelt den explizit eingefrorenen gemeinsamen
GPT-/MBR- und FAT32-/NTFS-Werkzeugstand. Recovery-Gastprogramme nutzen ihn mit
storage_tools_guest.Target und echten R4SYS-Claims. Auch der eingefrorene
ImageCreator nutzt diesen Baustein; sein bisheriger lokaler NTFS-Builder
entfaellt. Vorbereitete Plaene/Metadaten und begrenzte Arbeitsbuffer vermeiden
volumengrosse RAM-Allokationen bei der Formatierung. Normale Runtime-Binaer-
importe bleiben unveraendert; Herkunft und SDK-/Distribution-Patches stehen
unter Provenance/owner-update-0.76.9.json und Legal/Changes.

Die 16 Standard-NTFS-Metadatenvorlagen sind bytegleich mit den bisherigen
Windows-erzeugten SDK-Fixtures und ihren Herkunftshinweisen. Produktkopien
und Hashreceipt liegen in Platform/SDK/r4os/storage_tools/ntfs_metadata.
Sie enthalten Dateisystemdaten, keinen Microsoft-Formattercode.

-Mode StorageToolsTest erweitert den vorhandenen PS7-Netzwerkpruefer um einen
entbehrlichen 128-MB-NVMe-Datentraeger. Der gekapselte /STORAGESMOKE TOOLS-
Nachweis verlangt zuerst die technische Bootfixture-GUID und eine passende
leere Scratchdisk. Er prueft MBR/Aktivmarkierung, CLEAN, GPT-Haupt-/Sicherung,
exklusive Partitionsclaims, FAT32-Quick, NTFS-Full und danach echte
Mount-/Datei-/Unmountoperationen. Bootmedium, RAM-C, SYSTEM und DATA bleiben
benutzbar; bestehende SSH/FTP-/Menuepruefungen laufen im selben SMP4-Gast.
Die Funktion ist kein oeffentlicher Partitionsbefehl; R4PART folgt separat.
Ergebnisdatei: Artifacts/BootProbe/storage-tools/network-results.json.

Hostseitig prueft der kanonische SDK-Komponentenbestand Geometrie, Ueberlappung,
Stale/Readrace, Kapazitaets- und Allokationsfehler, Schreibabbruch, Flush- und
Readbackfehler sowie Quick/Full. Ein 128-GB-NTFS-Formatter laeuft mit insgesamt
2 MB Formatter-Allocator inklusive Arbeitsbuffer; die virtuelle Platte hat
separaten begrenzten Metadatenspeicher. Bestehende NtfsVerify-Pruefungen
validieren die kleinen realen Hostdateien weiterhin unabhaengig.

0.76.10: R4PART als gemeinsame Konsolenanwendung
------------------------------------------------
R4OSDev/r4os-app-r4part besitzt R4PART.R4X, IMAGE_SCOPE=slim und den normalen
Terminalpfad. Recovery importiert den beim Besitzer gebauten Stand 0.1.1
samt Quellarchiv, Manifest, Lizenzen und Hashes. Manage Partitions startet
ihn direkt; EXIT kehrt ins Menue zurueck. Im Terminal kehrt EXIT zum Prompt
zurueck. R4SYS plus die R4DESK-Konsolenfunktionen bilden seine Plattformbasis.

Die interaktive Auswahl bindet Geraet/Generation/Partition; Geometrie und
Metadaten werden vor I/O vorbereitet. CREATE, DELETE, CLEAN, CONVERT und
FORMAT zeigen zuerst das konkrete Ziel und verlangen YES DISK n, fuer eine
Partition zusaetzlich PARTITION n. Konvertierung ist nur fuer leere Tabellen
moeglich. ASSIGN/REMOVE und ONLINE/OFFLINE nutzen den gemeinsamen Mountzustand.
OFFLINE bleibt nach EXIT bestehen; ONLINE bindet explizit erneut ein.
Geaenderte Datentraeger-/Partitionskennungen erben keine alten Laufwerks-
zuordnungen oder Installationsrollen. Echte Dateisystemfehler bei gleicher
Identitaet bleiben Remountfehler. Normale und eingefrorene Storagebesitzer
sind ueber den expliziten 0.76.10-Quellpatch dokumentiert.

Ein gebuendelter Parserfall prueft Argumentgrenzen, Tippfehler, Einheiten,
Ueberlaeufe und Zielbestaetigungen. -Mode R4PartTest fuehrt die echten Befehle
im vorhandenen Netzwerkpruefer aus, mit 128 MB NVMe nur fuer diese Abnahme.
Der normale Full-Gast nutzt denselben Bestand und eine private QEMU-Snapshot-
Konfiguration fuer den lokalen Terminalstart. Kein Benutzer-Sichttest.

SSHD 0.1.6 liest Konsolenzustand und Transcript unter einem gemeinsamen
Revisionsvergleich. Bei zwischenzeitlichem Schreiben bleiben die Sendecursor
unveraendert, bis beide Abfragen denselben Stand beschreiben. Der normale
R4PART-Gast deckt die zuvor beobachtete doppelte Ausgabe mit ab.

R4PART ist auch mit den veroeffentlichten Paket-Pins ohne Workspace-Forks
baubar. Das Artefakt ist bytegleich mit dem regulaeren Besitzerbuild. Der
primaere lokale Terminalhost nutzt dieselbe physische Tastaturwarteschlange
wie seine Konsolenkinder; gehostete SSH-/Menuesitzungen bleiben getrennt.

Abnahme 0.76.10: Linux/KVM SMP4, 1024 MB. Recovery BIOS/VirtioNet 50.343 s,
UEFI/RTL8139 47.4 s, offline/beschaedigtes SYSTEM 10.312 s.
Normaler Full-Gast einschliesslich lokalem Terminal 58.015 s;
USB-Menuepruefung BIOS/1024x768 15.85 s mit 16 Framebuffervergleichen.
Alle bestanden; normaler testquick und gebuendelte SDK-/R4PART-/SSHD-Hosttests
ebenfalls. 899 gepinnte Dateien, 38 Runtimeimporte, 20 lokale Revisionen.
Stand: Recovery/Recovery-Kernel 0.1.8, Menue 0.1.3, R4PART 0.1.1,
SSHD 0.1.6, normaler Kernel 0.1.92. Kein Windows-/Lenovo-Lauf.

0.76.11: NTFS-Vergrößerung
-------------------------
R4PART 0.1.2 enthält EXTEND [SIZE=MB]. Der explizit übernommene SDK-Plan
vergrößert nur angrenzendes NTFS bei unverändertem Start und Kennungen.
Bitmaprelokation, Dateisystemmetadaten und Tabellen werden vorab geprüft,
unter exklusivem Claim erneut validiert und mit Flush/Readback geschrieben.
Nach Erfolg wird der vorherige Laufwerksbuchstabe mit frischer Identität
wieder eingebunden. Unterstützte Geometrie und Unterbrechungsgrenzen stehen
in Platform/SDK/DOCUMENTATION.de.txt. Kein Kernelmechanismus wurde erweitert.
Die vorhandene R4PART-Abnahme prüft zusätzlich Busy durch ein offenes SFTP-
Handle, fehlenden Nachbarplatz, FAT32-Abweisung und einen 40-MB-Datentransfer
auf dem von 32 auf 48 MB vergrößerten Volume samt Hashvergleich.

Abnahme 0.76.11: Linux/KVM, SMP4, 1024 MB. BIOS/VirtioNet 81.128 s,
UEFI/RTL8139 83.174 s, offline/beschaedigtes SYSTEM 10.230 s.
Normaler Full-Gast 88.005 s samt lokalem Terminal. Alle bestanden.
Die 40-MB-SFTP-Datei wird hoch- und wieder heruntergeladen; fuer diesen
80-MB-Rundlauf gilt eine eigene 60-s-Clientfrist (sonst weiterhin 20 s).
SDK 6/6, R4PART-Parser 1/1; Standalone-Build mit oeffentlichen Pins bytegleich.
R4PART SHA-256: 24dd8217acec684fed2d68713ca1c499ea995faa82b7895a1e8e16bf22092098.
901 gepinnte Dateien, 38 Module, 21 lokale Revisionen; Recovery/Recovery-
Kernel 0.1.9, R4PART 0.1.2; normaler Kernel 0.1.92 unveraendert.
Keine Windows-/Lenovo-Ausfuehrung.

0.76.12: NTFS-Verkleinerung mit tatsaechlicher Schrumpfgrenze
-----------------------------------------------------------
R4PART 0.1.3 bietet SHRINK QUERYMAX und SHRINK [DESIRED=MB]. Beide Richtungen
verwenden den gemeinsamen ntfs_resize-Plan. Die echte Bitmap und MFT-Runlists
bestimmen die Grenze; Dateidaten/sonstige Metadaten bleiben fest, nur die Bitmap
kann im Volume umziehen. Die Allocation muss zu den MFT-Runs passen; der gesamte
MFT-/Bitmaplesestand wird vor Schreibbeginn erneut geprueft. Keine geschaetzte
Grenze aus dem freien Gesamtplatz und keine vollstaendige Defragmentierung.
Die kleinere FS-Geometrie wird vor der kleineren Tabelle haltbar gemacht.
Dateisystem, Tabelle und anschliessender Remount muessen zusammen erfolgreich
sein. Grenzen/Unterbrechungsfenster: Platform/SDK/DOCUMENTATION.de.txt.
Die vorhandene R4PART-SMP4-Folge erweitert den Grossdateinachweis um QUERYMAX,
abgewiesenes SHRINK bei grosser Datei/offenem Handle und 48->16 MB nach deren
Loeschung, vorhandene Daten und anschliessende neue Schreibzugriffe.

Abnahme 0.76.12: Linux/KVM, SMP4, 1024 MB. Bios/virtio-net-pci 94.191 s, Uefi/rtl8139 94.428 s, Bios/offline 10.199 s.
Normaler Full-Gast 100.876 s samt lokalem Terminal; alle bestanden.
SDK 7/7, Parser 1/1; oeffentlicher Standalone-Build bytegleich.
903 gepinnte Dateien, 38 Module; Recovery/Recovery-Kernel 0.1.10,
R4PART 0.1.3, normaler Kernel 0.1.92 unveraendert.
Neue Allokation nach SHRINK wird via SFTP unter neuem Namen geprueft;
Ueberschreiben/Trunkierung ueber natives COPY, jeweils mit SHA-256-Vergleich.
Ein erster paralleler Normal-Lauf scheiterte beim SFTP-Grossdateischreiben;
der separate Lauf mit zusaetzlicher Fehlerdiagnose bestand unveraendert.
Ein UEFI-Starttimeout beim SSH-Banner wurde separat erfolgreich wiederholt.
Diese Erstfehler werden nicht als bestandene Laeufe gewertet.
Keine Windows-/Lenovo-Ausfuehrung.

0.76.13: GPT-Reparatur und R4PART-Skripte
---------------------------------------
R4PART 0.1.4 und der gemeinsame SDK-Stand sind ausdruecklich uebernommen.
CHECK GPT prueft beide Kopien; REPAIR GPT schreibt ausschliesslich die defekte
Kopie aus einer eindeutigen intakten Gegenkopie mit Flush/Readback. MBR,
Partitionen und Gegenkopie bleiben erhalten. Unklare/ungueltige Quellen oder
I/O-Fehler werden nicht als Erlaubnis fuer ein geratenes Layout verwendet.
/S laedt maximal 64 KB UTF-8/BOM vorab in RAM, verwendet den normalen Parser
und verlangt jede Bestaetigung als ausdrueckliche Zeile. Erster Fehler stoppt
mit Exitcode 1; EXIT/Erfolg liefert 0. HELP enthaelt Vertrag und Beispiele.
Die vorhandene Gastfolge beginnt jetzt mit defektem GPT-Header oder Array,
repariert ihn mit der echten R4PART-Datei und prueft Skriptabbrueche/Fehlercodes.
Unveraenderte Resize-/Dateiwitnesses bleiben Bestandteil desselben Ablaufs.

Der normale Kernel 0.1.93 und der explizit uebernommene Recovery-Kernel
verwenden jetzt Heap-Arbeitsbuffer je NTFS-Mount. Die vorherige globale
Record-/Attribut-/Index-/Kompressionsablage passte nicht zu den unabhaengigen
fs-request-Sperren der Laufwerke. Paralleles SYSTEM-I/O konnte so einen
Dateischreibtransfer auf einer zweiten NTFS-Partition mit not_found abbrechen.
Mountfehler und letzter Unmount geben den Buffer frei; freie Slots belegen
keinen Scratchspeicher, und beim Initialisieren entsteht keine grosse
Stackkopie. Der normale Gasttest schreibt zusaetzlich 16 MB nach C:, waehrend
der 40-MB-Test auf Y: laeuft, und vergleicht beide gelesenen Dateien per SHA-256.
Der erweiterte Test scheiterte vor der Korrektur auf Kernel 0.1.92; mit
Kernel 0.1.93 bestanden beide Hashpruefungen und die gesamte R4PART-Folge.

Kernel 0.1.94 ergaenzt Fehlerdiagnose, ohne Erfolgswege umzudeuten: Bei einem
fehlgeschlagenen Stream-Schreiben stehen Phase (1 Elternpfad, 2 Dateieintrag,
3 Besitzslot, 4 Slotzustand/Offset, 5 Volume, 6 Append, 7 Nachschlagen,
8 Nachhergroesse), Offset, Laenge und bestehende Cache-Fehlerzaehler im Log.
Die gemeinsamen NTFS-Unterphasen sind nur ein Diagnosehinweis, da parallele
Volumes sie weiterschalten koennen. Ein beobachteter UEFI-I/O-Abbruch (-5,
30474240 Bytes) ist vom behobenen Scratchkonflikt (-3/not_found) getrennt.
Das nach Abbruch gesicherte 48-MB-Volume war konsistent; drei gezielte
40-MB-Rundlaeufe und die komplette UEFI-Folge reproduzierten den I/O-Fehler
nicht. Kein behaupteter I/O-Fix: Problems.txt und 0.76.24/6 bewahren den Befund
fuer die Schlussintegration. Kein automatischer Retry in der Abnahme.

Abnahme 0.76.13: Linux/KVM, SMP4, 1024 MB. Bios/virtio-net-pci 122.929 s, Uefi/rtl8139 122.943 s, Bios/offline 10.324 s.
Getrennte BIOS-/UEFI-Laeufe sind ueber gleiche Kernel-/Runtime-Hashes gebunden.
Normaler Full-Gast 166.191 s samt 16-MB-C:/40-MB-Y:-Paralleltransfer,
beiden Dateihashes, lokalem Terminal und /S-Rueckweg. Alle finalen Laeufe
bestanden; frisches Testimage/testquick, SDK 8/8 und Parser/Skript 2/2 ebenfalls.
Oeffentlicher R4PART-Standalone-Build bytegleich. 906 gepinnte Dateien,
38 Module; Recovery/Recovery-Kernel 0.1.11, R4PART 0.1.4, normaler Kernel 0.1.94.
Keine Windows-/Lenovo-Ausfuehrung. Fehlversuche sind oben getrennt dokumentiert.


0.76.14: Installationsziele und Betriebssystemanzeige
---------------------------------------------------

RecoveryTools/Menu besitzt die gemeinsamen Modelle fuer ganze Datentraeger,
SYSTEM-Partition und RECOVERY-Installation. Ein Inventarsnapshot bindet
Geraetereferenz/-generation, Layoutgeneration, Sektoren und GPT-IDs. Modell,
Kapazitaet, Ziel-GUID und bei Updates Partitionsnummer sind sichtbar.
Eine unpartitionierte Platte besitzt innerhalb der Sitzung die gepruefte
Geraetereferenz; es wird keine erfundene persistente UUID angezeigt.

Die reale R:-Zuordnung bestimmt die Startquelle. Lokal bleiben USB-Medien
aus allen Auswahlwegen ausgeschlossen, waehrend die eigene lokale Platte
fuer Install erlaubt ist. Beim USB-Start bleibt genau der gestartete Stick
aus Install ausgeschlossen, aber fuer Update Recovery erreichbar. Ohne
bestaetigte Bootquelle gibt es keine schreibenden Ziele. RAM, schreibgeschuetzte
Medien, nicht unterstuetzte Sektorgroessen, unvollstaendige Inventare und aktive
Claims sind keine Ziele.
Install verlangt Platz fuer das feste Layout einschliesslich mindestens
16 MB NTFS-DATA und der Backup-GPT am echten Ende.

Updates verwenden den unveraenderten strikten Parser aus
Kernel/storage/installation.zig als ausdrueckliches Zig-Buildmodul. Jede der
fuenf Rollen wird danach gegen das tatsaechliche ABI-Inventar und genau das
BOOT-Volume gebunden, aus dem die Datei gelesen wurde. Fehlende SYSTEM-Dateien
verhindern damit keine Reparatur einer weiterhin eindeutig zugeordneten
Installation. Widerspruechliche Manifeste, unvollstaendige Tabellen, doppelte
Disk-/Partition-GUIDs und wiederholte Installations-IDs autorisieren kein
Update. Die Auswahl meldet ausgeschlossene ungueltige/mehrdeutige Installationen.
Eine vollstaendige Neuinstallation darf auch fremde/defekte Layouts ersetzen.

Die Erkennung liest ausschliesslich bereits eingebundene FAT32-/NTFS-Volumes
unter einem Storage-use-Lease; explizit mit R4PART OFFLINE gesetzte Partitionen
werden nicht heimlich wieder eingebunden. Fehlende/unsupported/unlesbare
Inhalte blockieren Install nicht. Eine unlesbare oder nicht eingebundene
BOOT-Partition liefert kein pruefbares Updateziel; bei Bedarf mit R4PART
einbinden und R im Zieldialog druecken. Inventarwechsel waehrend des Scans
verwerfen den Snapshot. Grenzen: 16 Geraeteslots, 128 Partitionen je Geraet,
27 Volumes entsprechend dem aktuellen Plattformvertrag; keine still gekuerzte
Zielliste bei Ueberschreitung.

OS-Merkmale sind rein informative Funde, keine Updateberechtigung:
- R4OS: passend gebundener BOOT-Vertrag, oder eine gueltige RELEASE_VERSION
  zusammen mit dem R4M0-Header der installierten TERMINAL.R4X.
- Windows: ntoskrnl.exe mit MZ-Header und SYSTEM-Hive mit regf-Signatur,
  alternativ Microsoft-EFI-Bootloader mit MZ und BCD mit regf.
- Linux: lesbarer x86-Kernel mit Bootflag 55 aa und HdrS-Kennung, etwa
  vmlinuz oder ein passender Eintrag unter boot (maximal 64 Eintraege).
  GRUB, ein Dateisystemtyp oder ein Label allein genuegen nicht.

Treffer mehrerer Volumes werden je Datentraeger als Windows, Linux, R4OS
zusammengefasst. Ohne Treffer bleibt der Namenszusatz leer. Das bedeutet
nicht, dass die Platte leer ist oder kein startfaehiges System enthaelt;
insbesondere werden nicht lesbare Linux-Dateisysteme nicht erraten.
Grundlagen fuer die lesenden Heuristiken:
https://docs.kernel.org/arch/x86/boot.html
https://learn.microsoft.com/en-us/windows/win32/sysinfo/registry-hives
https://projectzero.google/2024/12/the-windows-registry-adventure-5-regf.html

Install zeigt ausdruecklich das geplante neue Layout mit vollstaendigem
Datenersatz. SYSTEM-Update nennt die vorhandene Partition, BOOT-Kernel und
den Erhalt von DATA, RECOVERY, IDs, Grenzen und limine.conf. Recovery-Update
nennt die vorhandene RECOVERY-Partition und den Erhalt von INSTALL, anderen
Partitionen, IDs, Grenzen und limine.conf. Quelle: fester Cache oder eigener
GitHub-Kanal. R4OS bleibt R:\INSTALL\RELEASE.ZIP; Recovery verwendet separat
R:\INSTALL\RECOVERY.ZIP und spaeter RECOVERY.PART. Keine Pfadeingabe noetig.

Continue baut das Inventar erneut auf und vergleicht Quelle, Generationen,
Zielbereiche, BOOT-Referenz und SHA-256 des vollstaendigen Manifests. Eine
Aenderung verlangt erneute Auswahl. Die spaeteren Schreiber muessen nochmals
unter ihren exklusiven Claims pruefen. In dieser Unterversion erfolgt noch
kein Paketdownload/Schreibauftrag; der Dialog benennt diesen Stand ausdruecklich.

Die bestehende view-test-Gruppe prueft zusaetzlich Bootpolitik, gebundene
Rollen/Grenzen/Typen, falsches BOOT, fehlende Rollen, Klone, doppelte
Installations-IDs, Generationen und OS-Marker. UITest benutzt dieselben
Produktpfade mit USB-Tastatur, source/targets/review und Back-Vorauswahl;
zusammengefasste und leere Anzeigen sowie die reale Identitaetspruefung
sind in den seriellen Markern beobachtbar. Keine neue manuelle Abnahme.

Abnahme: 0.76.14: Linux/KVM SMP4,1024MB, USB keyboard/i8042=off. BIOS USB 640x480 19.092s,800x600 19.131s,1024x768 19.566s,1920x1080 20.222s; UEFI USB1024x768 20.795s; BIOS LOCAL1024x768 18.523s. Six passes/150 framebuffer comparisons, real target identity revalidation and default Back, combined Windows/Linux/R4OS markers, blank extra display, local USB exclusion and own-local eligibility. Existing Terminal/R4PART, independent console and reset passed. Host group6/6. No Windows or Lenovo run.

0.76.15: Releasepakete und vorbereiteter RAM
------------------------------------------

Recovery und R4OS besitzen getrennte Schema-1-Paketmanifeste; Details stehen
in RecoveryTools/Contracts.de.txt. Tools/Package.ps1 erzeugt das unabhängige
Recovery-ZIP. Distribution besitzt Tools/ReleasePackage.ps1 fuer das neue,
mit Recovery gepaarte R4OS-ZIP. Die produktiven Veroeffentlichungsstarter
werden in den folgenden Unterversionen an diesen Erzeuger angeschlossen.

R4ZIP.R4P 0.1.1 ist ein regulaeres oeffentliches R4OS-Protokoll und der 39.
eingefrorene Runtimeimport. Stored, Deflate, benoetigtes ZIP64, Eintrags- und
Pfadgrenzen sowie CRC32 gehoeren diesem Besitzer. Die gemeinsame SDK-Fassade
enthaelt nur den Aufrufvertrag. Der korrigierte Zig-Deflate-Baustein begrenzt
auch beim Wechsel von Huffman- zu Stored-Bloecken jeden Ausgabeschritt.
Originalherkunft, Lizenz und geaenderte Quellen werden im Paket mitgefuehrt.

Das Menue beschreibt ausschliesslich den passenden festen Cache auf R:.
Die schnelle Anzeige unterscheidet fehlend, ungeprueft, ungueltig und
geprueftes Manifest mit Version; sie bezeichnet noch keine vollstaendige
Nutzdatenpruefung. Continue liest das Original erneut, bindet dessen SHA256
an die angezeigte Quelle und prueft alle Dateien, Images und das gepaarte
Recovery. ESC beendet die Vorbereitung vor einer Zielaenderung.

Jede Vorbereitungsallokation verwendet den neuen resident-VM-Commit des
normalen Kernelbesitzers. Erfolg bedeutet genullte, physisch vorhandene und
bis zur Freigabe gepinnte Seiten. Aufrufe und Freigaben erfolgen in 256-KB-
Schritten mit Yield und sichtbarem Fortschritt. Das bestehende flags=0 bleibt
lazy. Programmlimits, physische Reserve und sichere TLB-Freigabe bleiben
verbindlich. Ein fehlgeschlagener Abbau kann wiederholt werden, ohne bereits
freigegebene Arenadaten ein zweites Mal zu durchlaufen.

Die Sitzung haelt das originale ZIP, alle entpackten Images, den vollstaendigen
SYSTEM-Dateibaum und den vorab berechneten NTFS-Zielplan bis zum Operationsende.
Sie benoetigt weder DATA noch eine temporaere Partition. Der Inhalt wird mit
dem gemeinsamen NTFS-Leser importiert und fuer die echte Zielgroesse vorbereitet;
Quellgrenzen werden nicht als Zielgeometrie uebernommen. Die gemessene Spitze
enthaelt auch Arenareserve und teilweise vorbereitete Allokationen bei OOM.
Erst nach erfolgreicher Vorbereitung darf ein spaeterer Schreiber beginnen.
In diesem Stand nennt der Dialog den noch ausstehenden Installationsvollzug.

Build.ps1 -Mode PackageTest baut Produkt- und vorhandenen UI-Diagnosekernel
sowie die Runtime. Tools/Test-Packages.ps1 verwendet die echten .NET-Erzeuger,
denselben ZIP-Kern und NTFS-Import auf dem Host sowie SMP4-Gaeste. HostOnly und
GuestOnly begrenzen einen Wiederholungslauf nach diesen Builds. Testimages
enthalten technische ELF-/SYSTEM-Fixtures und sind keine R4OS-Releases.

Abnahme: Linux/KVM BIOS mit vier vCPUs und USB-Tastatur. 4096 MB: 91.410 s,
Recovery-Paket und Menueweg, falscher SHA256, R4OS-Quelle nach physischer USB-
und Backendentfernung vollstaendig per SHA256 erhalten. 1024 MB: 21.549 s,
kontrollierter OOM mit erfolgreichem Abbau. Die Hashes der 2-GB-Zieldatentraeger
blieben identisch. Vorbereitete RAM-Spitze: R4OS-Fixture 3416813568 Bytes,
Recovery-Fixture 262656000 Bytes, jeweils einschliesslich Arenareserve.
Der Hostimport prueft alle sieben Dateien/Verzeichnisse und Dateiinhalte auf
einer neuen 32-MB-NTFS-Geometrie; ntfsfix -n bestaetigt deren Struktur. R4ZIP
6/6, eigenstaendiger Protokollbuild mit oeffentlichen Abhaengigkeiten und
bytegleichem R4P. Normaler Kernel 0.1.95: frisches Testimage und SMP4-testquick
einschliesslich TLB-/Lock-/Heapnachweisen bestanden. Windows/Lenovo ungetestet.

0.76.16: GitHub und persistenter Release-Cache
-------------------------------------------

Das Quellenmenue kann nun ueber den bestehenden Webtransport ein passendes
veroeffentlichtes Paket beziehen. R4OS und Recovery benutzen getrennte
GitHub-Endpunkte, feste Assetnamen, Version/Architektur und SHA256-Digests.
R4OS waehlt Slim/Full mit Links/Rechts. Die Downloadroutine folgt nur
freigegebenen HTTPS-Hosts und verwendet DNS, TCP, R4HTTP und das unveraenderte
R4TLS 0.2.5. Die fuer die beobachteten GitHub-Zertifikatsketten erforderlichen
USERTrust-RSA-/ECC-Wurzeln wurden im Distribution-Besitzer ergaenzt und
gezielt eingefroren. Herkunft, DER-Hashes und oeffentliche Zertifikate sind
im Trust-Katalog beziehungsweise in den Lizenz-/Provenance-Dateien enthalten.

Originale ZIPs bleiben ausschliesslich R:/INSTALL/RELEASE.ZIP und
R:/INSTALL/RECOVERY.ZIP. Ein neues PART muss fertig geschrieben, geflusht
und vollstaendig im RAM validiert sein, bevor eine Austauschtransaktion
beginnt. Groesse, Asset-SHA256, ZIP/CRC, Manifest und jeder Nutzdatenhash
werden geprueft. Ein Download benoetigt Platz neben dem alten ZIP und
1 MB Verwaltungsreserve; DATA wird nicht als Ausweichspeicher benutzt.

RecoveryTools besitzt das kleine R4CACHE1-Journal und die Wiederanlaufpolicy.
Der Kernel besitzt weiterhin die gepruefte FAT-Uebernahme unter einem
Dateisystemgate. Das neue additive Contract-Flag erlaubt nur fuer diesen
Vorwaertspfad einen langen Stagingnamen. Die kurze Eigentumseintragung
wird vor den LFN-Slots entfernt und geflusht. Unvollstaendige LFN-Anlage
oder -Loeschung darf beim Wiederverwenden freier Slots keinem anderen
Dateinamen zugeschlagen werden. Namen, Statusfolge, Generationen und
Fehlerzustaende stehen in RecoveryTools/Contracts.de.txt.

Die normale FAT32-Dateikettenverwaltung wurde ebenfalls korrigiert:
Freigabe und zufaellige Dateioffsets folgen der realen Volumegrenze statt
einer festen 4096-Cluster-Grenze. Fehler von FAT-/FSInfo-Schreiboperationen
werden gemeldet. Der vorherige Code konnte grosse geloeschte ZIP-Enden
belegt lassen oder einen entfernten Offset auf einen frueheren Cluster
abbilden. Die Aenderung gilt fuer den normalen Kernel und dessen bewussten
Recovery-Import. Das ist kein allgemeines FAT-Journal; eine Unterbrechung
innerhalb der Clusterfreigabe oder ein zerrissener Sektor kann weiterhin
eine Dateisystemreparatur erfordern.

Build.ps1 -Mode DownloadTest baut die Produktartefakte. Der gemeinsame
PowerShell-Lauf erzeugt gueltige, bewusst beschaedigte und alte Original-
ZIPs selbst, startet SMP4-Gaeste und nutzt die echte Guest-Downloadroutine.
Er prueft getrennte Kanaele, Redirect, Netzabbruch, falschen SHA256,
ungueltige Nutzdaten, Platzmangel und ein identisches Paket. Echte QEMU-
Abbrueche erfolgen nach dauerhaftem Journal, veroeffentlichtem ZIP und
entfernter Sicherung; synthetische FAT-Alias-/LFN-Zustaende ergaenzen diese
Grenzen. Ein 4100-Cluster-Dateitest prueft kalte zufaellige Offsets,
Schreiben, Nachbardaten und die Rueckgabe des gesamten freien Platzes.
Ein unabhaengiger Hostdurchlauf vergleicht beide FATs, alle erreichbaren
Dateiketten und den Freispeicherzaehler. Ein zusaetzlicher physischer
Installationsdatentraeger muss bytegleich bleiben. Es werden nur technische
Dateiimages beschrieben; vollstaendige oeffentliche Releasekanal-Abnahmen
folgen nach der geplanten Release-Integration.

Abnahme: Alle acht Downloadgruppen bestanden unter Linux/KVM BIOS mit vier
vCPUs und 4096 MB RAM, einschliesslich drei echten QEMU-Abbruechen und vier
FAT-Alias-/LFN-Fixtures. Nach jedem Lauf waren FAT-Spiegel, Erreichbarkeit
aller belegten Cluster und FSInfo konsistent; der separate 2-GB-Zieldatentraeger
blieb SHA256-identisch. Echter GitHub-API-/Redirect-TLS-Lauf bestanden.
Normaler Kernel 0.1.96: Build, frisches Testimage und SMP4-testquick bestanden.
Contractgenerator 26 Mutationen, SDK-Webtransport 40/40 und Menu 8/8 bestanden.
Der bestehende Menue-Gasttest BIOS/USB-Tastatur/1024x768 bestand in 19.98 s
mit 28 Framevergleichen inklusive Profilwechsel und netzloser Rueckkehr.
Recovery/Recovery-Kernel 0.1.14, Menu 0.1.6. Windows/Lenovo nicht ausgefuehrt.
Der Menue-Gasttest korrigierte nach dem Downloadlauf die neuen Links-/Rechts-
Tastencodes auf 0x88/0x89. Dieser separat erneut gepruefte Eingabezweig ist die
einzige Produktquellenabweichung gegenueber dem festgehaltenen Downloadlauf;
Download-, Paket-, Transport- und FAT-Quellen sind SHA256-identisch geblieben.

0.76.18: Installation und lokaler Selbstersatz
-------------------------------------------

Install R4OS fuehrt die bereits bestaetigte vollstaendige Datentraeger-
Ersetzung aus. Der feste ZIP-Cache bleibt Quelle und wird als unveraendertes
Original in das neue RECOVERY uebernommen. Innere GPT-/BOOT-Beschreibung,
aeussere BOOT-Dateien, SYSTEM-Katalog, eingebettete Kernelversionen und beide
Recovery-Slots werden vorab auf Uebereinstimmung geprueft. Die normale SDK-
Einheit besitzt jetzt auch den vorbereiteten FAT-Dateibaum des ImageCreator.
RecoveryTools besitzt ausschliesslich die Installationsfolge und Oberflaeche.

Alle Quelldaten und Plaene liegen vor dem Claim in gepinntem RAM. Die echte
Zielgroesse bestimmt DATA; sieben frische IDs binden die fuenf Partitionen,
das Installationsmanifest und das lokale Limine-Menue. BOOT, SYSTEM, beide
Recovery-Slots, ihre vollstaendigen Legal-Dateien, das Original-ZIP und frische
DATA-Verzeichnisse werden geschrieben und rueckgelesen. Der BIOS-Bootcode
folgt zuletzt. Der Abschluss verlangt Flush, neue gepruefte Mountidentitaeten
und einen erneuten SHA-256-Lauf ueber das installierte Original-ZIP.

Die lokale Neuinstallation darf das eigene physische Medium ersetzen. C:
bleibt RAM; der normale Kernelbesitzer erlaubt die ausdrueckliche neue R:-
Zuordnung ausschliesslich fuer die bei Recovery-Boot reservierte physische
Geraeteinkarnation. Ein Fehler vor Schreibbeginn stellt fruehere Mounts
wieder her. Nach dem ersten Schreibversuch bleiben fehlgeschlagene Ziele
unvollstaendig und ausgehaengt; SSH und Terminal bleiben fuer Diagnose nutzbar.
Der Ablauf ist keine atomare Installation ueber Stromausfaelle hinweg.

Die erste echte Installation deckte einen Fehler des normalen FAT-Lesers
auf: Ein Volume-Label BOOT verdeckte das Verzeichnis boot. Lookup, Mutation
und Aufzaehlung ueberspringen nun solche Metadateneintraege. Die Aenderung
wurde beim normalen Kernel vorgenommen und ausdruecklich importiert. Der
Installer behält die Distribution-Bezeichnung R4OS BOOT bei, damit auch
aeltere gueltige Release-Kernel starten. Die Gastfixture prueft dagegen
gezielt die Kollision BOOT/boot mit dem korrigierten Recovery-Kernel.

Tools/Test-Install.ps1 verwendet den echten Menueweg und physische Schreib-
zugriffe auf ausschliesslich temporaere QEMU-Dateiimages mit vier vCPUs.
USB/BIOS installiert auf ein groesseres 3-GB-NVMe-Ziel; lokal/UEFI ersetzt
das eigene 2-GB-Medium. Ein weiterer beschreibbarer Datentraeger und die
USB-Quelle werden danach per SHA-256 verglichen. Ein QEMU-Blockfehler und
1024 MB RAM pruefen Fehler nach und vor Schreibbeginn. Alle vier Faelle
verlangen anschliessend SSH-Antwort und reguläres Herunterfahren im Terminal.
Erfolgreiche Ergebnisse werden vom unabhaengigen Distribution-Pruefer
geprueft und jeweils als normales R4OS und Recovery gebootet.

Die technische Quelle ist das vollstaendige 0.76.17-Slim-Paket mit normalem
Kernel 0.1.97 und Recovery 0.1.14. Der neue Installer laeuft mit Recovery/
Recovery-Kernel 0.1.15 und Menu 0.1.7; der normale Besitzerkernel ist 0.1.98.
Die gemessene Vorbereitungs-Spitze betraegt 3628724224 Bytes (rund 3,38 GB)
bei 8192 MB Gast-RAM. Das ist ein Messwert dieser Quelle, keine allgemeine
RAM-Untergrenze. Windows und Lenovo werden hier nicht manuell getestet.

Abnahme: Alle acht Linux/KVM-SMP4-Faelle bestanden. USB-Installation 114,04 s,
lokaler Selbstersatz 76,88 s, Schreibfehler 103,28 s und RAM-Abweisung 79,17 s.
Vier anschliessende BIOS-/UEFI-Starts von normalem R4OS und Recovery mit
2048 MB bestaetigten die tatsaechlichen neuen Installationsidentitaeten und
regulaeren Terminal-Poweroff. Andere Datentraeger/USB-Quelle blieben
bytegleich; bei RAM-Abweisung auch das Ziel. SDK 10/10 Gruppen und die
Distribution-Besitzerpruefungen bestanden. Ein unabhaengiger vollstaendiger
Recovery-Baum in einem Pfad mit Leerzeichen baute Kernel und Runtime
SHA-256-identisch. Der beobachtete SeaBIOS/NVMe-Startfehler mit 4/8 GB liegt
vor dem Kernel und bleibt in Problems.txt fuer die Integrationspruefung
festgehalten; die lokale 8-GB-Installationsabnahme verwendet UEFI.

SYSTEM-Ersatz bei vorhandener Installation (0.76.19)
---------------------------------------------------

Update R4OS verbindet die ausgewaehlte SYSTEM-Partition mit ihrer BOOT-
Partition ueber Installationsmanifest und echte GPT-Identitaeten. Quelle
bleibt R:\INSTALL\RELEASE.ZIP beziehungsweise der vollstaendig gepruefte
Ersatzdownload. Das neue SYSTEM wird mit allen NTFS-Metadaten auf seine
bestehende Groesse vorbereitet. Vom 1024-MB-Standard abweichende Ziele
sind erlaubt, wenn der Inhalt passt. Alte SYSTEM-Dateien und Einstellungen
verschwinden; GPT, Partitionsgrenzen, Identitaeten, DATA und RECOVERY bleiben.

BOOT wird dateiweise erneuert. Normales Kernelartefakt, Preload und Begleiter
stammen aus demselben Release. limine.conf bleibt bytegleich, ebenso fremde
Bootdateien und deren Namen/Attribute. Der vorhandene normale Limine-Eintrag
muss die kanonischen Kernel-/Preload-Verweise und Modulkennungen enthalten;
weitere Menueeintraege, Einstellungen und Kommentare sind erlaubt. Die
installierte BIOS-Kette muss dem unterstuetzten Limine-Stand entsprechen.

Der gemeinsame SDK-Plan prueft die gesamte FAT-Belegung und bereitet alle
Aenderungen privat vor. Zwei getrennte exklusive Claims koordinieren BOOT
und SYSTEM. Unter ihnen werden GPT/Bootkette und der komplette SHA-256 des
vorbereiteten BOOT-Ausgangsstands erneut gelesen, bevor SYSTEM ersetzt wird.
Alle Schreibschritte verlangen Flush und Readback. Erfolgreicher Abschluss
prueft Mounts und neues Installationsmanifest. Ein Abbruch nach Schreibbeginn
wird als unvollstaendig gemeldet; Recovery arbeitet weiter aus RAM. Es gibt
keinen atomaren Rollback bei Stromverlust.

Tools/Test-SystemUpdate.ps1 verwendet nur entbehrliche QEMU-Dateiimages mit
vier vCPUs. Ein 768-MB-SYSTEM wird aus einem vollstaendigen Release ersetzt;
ein gueltiges, im Host vorbereitetes 16-MB-SYSTEM muss vor Schreibbeginn
abgewiesen werden. Die Fixture verkleinert dazu ein frisches 32-MB-NTFS. Ein
unabhaengiger FAT-/GPT-Pruefer vergleicht Geometrie, fremde BOOT-Dateien,
eigene Limine-Konfiguration und die vollstaendigen DATA-/RECOVERY-Hashes.
Ein lesender NTFS-Nachweis vergleicht jeden Releasepfad und jede Datei sowie
die genaue Anzahl von Kindern je Verzeichnis. Der normale Ergebnisboot
muss die bisherige Installation zuordnen und im Terminal herunterfahren.

Abnahme: Alle drei Linux/KVM-SMP4-Faelle bestanden: System768 126.309 s, SystemTooSmall 105.706 s, UpdatedNormalBoot 29.347 s.
Quelle 0.76.18 mit normalem Kernel 0.1.98 und festem Recovery 0.1.14;
Ausfuehrung Recovery/Kernel 0.1.16 und Menu 0.1.8. Vorbereitungs-Spitze
3628724224 Bytes. SDK 11/11 und Menu 9/9 Gruppen, Produktions-/UI-Build
und unabhaengige Inhalts-/Strukturpruefung bestanden. Keine Windows- oder
Lenovo-Abnahme; der Hardware-Sichttest bleibt dem Roadmapabschluss vorbehalten.

Recovery-Selbsterneuerung (0.76.20)
---------------------------------

Update Recovery verwendet R:\INSTALL\RECOVERY.ZIP und denselben unabhaengigen
GitHub-Quellenweg wie die vorhandene Quellenwahl. Ziel ist genau eine anhand
von BOOT-Manifest und GPT gebundene RECOVERY-Partition. USB-Boot erlaubt auch
den gestarteten Stick selbst; beim lokalen Boot bleiben USB-Medien verborgen.

Das vollstaendige neue Paket, die bisherigen Slotinhalte und alle Schreib-
und Pruefpuffer werden vor dem exklusiven Claim im RAM bereitgestellt.
RECOVERY wird nicht formatiert. Zwei getrennte gemeinsame FAT-Plaene sichern
zuerst ein bestaetigtes CURRENT nach PREVIOUS und ersetzen erst nach dessen
Flush/Readback CURRENT. Beide enthalten die exakten Manifestdateien inklusive
Legal; entfernte Dateien bleiben nicht als Altbestand im Slot zurueck.
INSTALL, andere Partitionen und limine.conf werden erhalten.

Die Startbestaetigung kommt aus dem initialisierten Menue. Der Kernel liefert
nur die tatsaechlichen Startidentitaeten: Original-ELF-Hash und -Laenge, den
vor dem RAM-Mount geprueften Runtime-Hash, Versionen, GPT-IDs und Slot. Das
Menue prueft sie gegen CURRENT und schreibt state.r4s mit Flush und Readback.
Ein Schreibfehler verhindert den RAM-Betrieb nicht. Unbestaetigtes CURRENT
ersetzt PREVIOUS nicht; beim Start aus PREVIOUS bleibt dieser Slot erhalten.
Ein neues Paket enthaelt zusaetzlich die explizite Kernel-/Runtime-Bindung in
der ELF-Sektion .r4os.recovery.pair. Der Updateweg prueft sie vor dem Schreiben.

Nach einem unterbrochenen CURRENT-Ersatz wird der feste Previous-Eintrag
manuell in Limine gewaehlt. Es gibt keine automatische Slotumschaltung und
kein allgemeines Stromausfalljournal fuer das gemeinsame FAT32. Bei defekten
gemeinsamen FAT-/BOOT-Strukturen ist weiterhin ein externer Stick erforderlich.

Tools/Test-RecoveryUpdate.ps1 verwendet ausschliesslich entbehrliche Images
und SMP4-Gaeste. Die technische UI-Fixture besitzt dieselbe Versionsnummer
wie das Produktionspaket, aber andere Kernel-/Manifest-Bytes. Sie darf nicht
veroeffentlicht werden. Der Hostnachweis verwendet die echte ZIP-Pruefung
und dieselben Slotplaene fuer Schreib-, Flush- und Ruecklesefehler sowie
unbestaetigte, falsche und von PREVIOUS gestartete Ausgangsstaende. Die
Gastfaelle pruefen Selbstupdate, produktiven Ergebnisstart, fehlgeschlagenes
Speichern der Bestaetigung und echten Schreibfehler mit manuellem Previous-
Start. Der begrenzte Lenovo-Sichttest bleibt dem Roadmapabschluss vorbehalten.

Abnahme 0.76.20: Alle fuenf Linux/KVM-SMP4-Faelle bestanden: UsbSelfUpdate 136.507 s, NewCurrentBoot 38.923 s, ReadonlyConfirmation 59.553 s, CutCurrent 140.828 s, PreviousAfterCut 72.761 s.
Recovery/Kernel 0.1.17 und Menu 0.1.9. Vorbereitungs-Spitze 3934666752 Bytes;
8192 MB fuer Selbstupdate/Fehler/Fallback, 2048 MB fuer normalen Neuboot und
Read-only-Bestaetigung. SDK 11/11, Menu 10/10 Gruppen und sechs Hostfehler
plus drei Erhaltungsfaelle bestanden. Der echte CURRENT-Payload-Schreibfehler
laesst die gesamte FAT-Belegung konsistent; manuell gewaehltes PREVIOUS kann
CURRENT anschliessend reparieren. Gemeinsame Metadaten-/Stromausfallgrenzen
bleiben bestehen. Keine Windows-/Lenovo-Abnahme.

0.76.23: Komponenten, Kapazitaet und RAM
--------------------------------------

Recovery/Kernel 0.1.19 und RECOVERY.R4X 0.1.11 verwenden weiterhin die
ausdruecklich eingefrorenen Treiber, Dienste, Protokolle und Konsolenmodule.
Die letzte SDK-Uebernahme ist allein die gepruefte NTFS-Mindestgrenze aus
0.76.21: DATA 32769 Sektoren, ganzes Medium 3444770 Sektoren. Herkunft,
Patch und Hashes stehen in Provenance/owner-update-0.76.23.json. Es gibt
keinen laufenden Import aus dem normalen Workspace.

RECOVERY bleibt 512 MB. Der echte FAT-Nachweis verwendet zwei komplette
Slots, RELEASE.ZIP/RELEASE.PART, RECOVERY.ZIP/RECOVERY.PART und zusaetzlich
1 MB Metadatenreserve. Er fuehrt den Slotwechsel aus, prueft saemtliche
Cacheinhalte und beide FATs unabhaengig und weist eine uebervolle Kombination
vor Schreiben ab. Tools/Test-Budgets.ps1 bindet seinen Bericht an die
konkreten Paket-/Quellimage-Hashes. Distribution prueft dieselbe Kombination
gegen die tatsaechliche FAT-Belegung; der Installer reserviert Platz fuer
die weiteren Archive in seiner vollstaendigen Vorabplanung. Eine volle
Partition wird nicht automatisch vergroessert.

Install/Update werden mit 8 GB Gast-RAM qualifiziert. minimumRamBytes
verlangt 7 GB OS-RAM aus der unverkuerzten Bootspeicherkarte: usable,
bootloader_reclaimable und kernel_and_modules. Es zaehlen nur ganze
4-KB-Seiten, ohne doppelte Bereiche, Adressluecken, reservierte Geraete-
fenster, Framebuffer oder Firmwarebereiche. Der Abstand zum 8-GB-System
laesst deren Reservierungen zu. Jeder konkrete Auftrag muss ausserdem
seine gesamte Vorbereitung physisch reservieren, bevor ein Zielclaim
oder ein Schreibzugriff beginnt. Weniger RAM kann fuer reine Reparatur-
terminals reichen; das sagt nichts ueber die Ausfuehrbarkeit eines Pakets.

Die RECOVERYRAM-Marker geben ram_capacity, den Menuebedarf und beobachtete
Spitzen bei jeder residenten Commit-Charge und beim Ende der Sitzung an.
pinned_peak ist der gesamte gleichzeitig festgehaltene Vorbereitungspool;
er umfasst auch Original-ZIP, entpacktes Image und Schreibplaene. Die
beobachtete Gesamtspitze enthaelt zusaetzlich Kernel, Dienste und Konsole.
Die alten PMM-Rohwerte vor dieser Korrektur enthalten Adressluecken und
duerfen nicht als verbrauchter Arbeitsspeicher interpretiert werden.

Normale Releasepakete enthalten auch die eigenstaendigen USB-Starter,
Tools, Quellenmanifest, README und QEMU-Konfiguration. Recovery prueft
deren portable Pfade, exakte Dateiliste, Groessen und Hashes wie alle
anderen ZIP-Inhalte. Diese Hostbeigaben bleiben im Original-ZIP; sie
werden nicht in SYSTEM oder BOOT installiert. Unbekannte Root-Dateien,
Traversalpfade und ein verwaltetes limine.conf bleiben unzulaessig.

Hardwareumfang: x86_64 in der AVX2-Klasse, Limine unter BIOS oder x64-UEFI,
512-Byte-Sektoren, die aufgenommenen AHCI/NVMe/ATA-/USB-Massenspeicherpfade
und PS/2-/USB-HID-Tastaturen. Netzwerk benutzt die aufgenommenen VirtioNet-,
RTL8139- oder RTL8168-Treiber samt DHCP/SSH/FTP. Ein vorhandener Treiber ist
kein Nachweis fuer jedes reale Geraet. Automatisierte Nachweise stammen
von Linux/KVM mit vier vCPUs; Windows-Hostzweige und echte Lenovo-Hardware
werden dadurch nicht als manuell geprueft ausgegeben. Nur der abschliessende
USB-Boot und die lokale Installation auf dem Lenovo sind als Benutzertest
vereinbart.

Abnahme 0.76.23: Linux/KVM SMP4. Die vollstaendige Ausgangsfolge bestand mit
8 Installations-, 3 SYSTEM-Update- und 5 Recovery-Updatefaellen. Anschliessend
wurde ausschliesslich die RAM-Vorpruefung und deren Messung korrigiert.
Mit dem abschliessenden Artefakt bestanden erneut USB-Installation,
SYSTEM-Ersatz, Recovery-Selbstupdate und deren produktive Ergebnisstarts,
zusaetzlich die neue 6-GB-Mindestanforderungsabweisung: 8 gezielte Gastfaelle.
Der 6-GB-Gast bereitete das ZIP vor, meldete InsufficientRam und liess den
gesamten Zieldatentraeger bytegleich. Die bestehende 1-GB-OOM-Abweisung,
lokale Neuinstallation, Erhaltungs- und Schreibabbruchfaelle der Ausgangs-
folge sind im Bericht getrennt an deren damaligen Artefakthash gebunden.
Es wird kein alter Bericht als Lauf des geaenderten Artefakts ausgegeben.

  UsbInstall: Menue 141.19 MB, beobachtete Spitze 3647.71 MB, gepinnt 3498.57 MB.
  RamMinimum: Menue 141.12 MB, beobachtete Spitze 3647.58 MB, gepinnt 3498.57 MB.
  System768: Menue 141.04 MB, beobachtete Spitze 3647.57 MB, gepinnt 3498.57 MB.
  UsbSelfUpdate: Menue 141.86 MB, beobachtete Spitze 3901.90 MB, gepinnt 3752.44 MB.

  512-MB-RECOVERY: 70475776 B / 67.21 MB frei nach beiden Slots, beiden Archivpaaren und 1 MB Reserve.
  R4OS-0.76.22-full-x86_64.zip: 91993612 B; SHA256 87201014bf832aa288a492fab52ea7d6d2fd88cc20289d334a5cf6867bd5f5fc
  R4OS-Recovery-0.1.19-x86_64.zip: 24684567 B; SHA256 b30a26c1294ed9c2b25e031af19ad77c933acd49feb9ba05661e05245017ce23
  Abschliessendes runtime.img: b44f14fa6f00990482a49254a25a9200193728f88f869c1dbd1cdb23dca74c8e


Der unabhaengige physische FAT-Nachweis, Slotrotation und negative
Kapazitaetsfaelle bestanden. Menu-Hostgruppe 12/12; eingefrorener SDK-Test
zur realen Mindestgeometrie bestanden. Inventar: 969 Dateien, 39 eingefrorene
Module, 56 Binaerimporte; die eigene RECOVERY.R4X besitzt ihre gesondert
geprueften Imports. runtime.img enthaelt 58 Dateien. Der aggregierte Bericht
steht unter Artifacts/Qualification/0.76.23/report.json und unterscheidet
Ausgangsfolge, abschliessende RAM-Korrektur und Paket-Hashes. Keine manuelle
Windows- oder Lenovo-Abnahme. Firmware-/Gesamtintegration folgt in 0.76.24.


Gesamtintegration 0.76.24
-----------------------
Die begrenzte Linux/KVM-SMP4-Auswahl bestand mit dem unveraenderten
Recovery-/Kernelstand 0.1.19, Menue 0.1.11 und runtime.img SHA256
b44f14fa6f00990482a49254a25a9200193728f88f869c1dbd1cdb23dca74c8e.
BIOS/lokal und UEFI/USB mit 8 GB, 1024x768, USB-Tastatur und deaktiviertem
i8042 bestanden alle sechs Menuepfade und je 28 Bildvergleiche in
19.607 beziehungsweise 21.483 s. Konsolenbild, Escape/EXIT, IP-Anzeige und
unveraenderte unabhaengige Sitzung sind enthalten; keine Benutzer-Preview.

Die zusammengefuegte Netzwerk-/Storage-/R4PART-Folge bestand unter BIOS
mit VirtioNet in 137.031 s und unter UEFI mit RTL8139 in 136.377 s.
SSH/SFTP/SCP/aktives/passives FTP, belegte Ziele, exklusive Ansprueche,
veraltete Identitaeten, echte Remount-/Flushfehler, GPT-Reparatur, Format,
NTFS EXTEND/SHRINK und 40-MB-Hashruecklesen sind gemeinsam abgedeckt.
Offline mit beschaedigtem SYSTEM blieb die lokale Konsole nutzbar.
Lokales CURRENT-Selbstupdate auf NVMe bestand in 82.45 s; PREVIOUS, Caches,
andere Partitionen und die laufende SSH-Sitzung wurden nachgeprueft.
Die komplette Downloadgruppe mit ungueltigen Paketen, Platzmangel, drei
Stromabbruechen und vier FAT-Zwischenzustaenden bestand erneut mit 8 GB.

Installations-, SYSTEM-Ersatz-, RAM- und weitere Recovery-Abbruchnachweise
aus 0.76.23 werden mit ihren jeweiligen Artefakthashes weiterverwendet;
alte Messungen werden nicht als neuer Lauf ausgegeben. Der Bericht unter
Artifacts/Qualification/0.76.24/report.json bindet diese Belege zusammen.
SeaBIOS 1.17/QEMU 11.0.2 benoetigt fuer NVMe-Boot mit 8 GB die gemeinsame
Vorgabe -fw_cfg name=opt/org.seabios/pci64,string=0. Der Gegenvergleich mit
identischem Full-Image belegt Firmware-BAR 0x7000000000 ohne Start gegen
0xfebd4000 mit erfolgreichem normalen R4OS-Boot in 25.020 s. Die gemeinsame
Guest-Qmp.ps1 stellt dies fuer UI, Install-/Update-Ergebnisboots bereit.
OVMF ignoriert diesen SeaBIOS-Schluessel. Kein Kernel-/Bootloaderpatch.

Der gesicherte alte NTFS-I/O-5-Einzelbefund trat in der begrenzten erneuten
BIOS-/UEFI-Folge nicht auf. Seine Ursache bleibt unbekannt; Problems.txt
unterscheidet ihn weiterhin vom behobenen alten NTFS-Scratchkonflikt und
den absichtlich injizierten Flushfehlern. Auch der historische UEFI-USB-
Startausfall trat nicht erneut auf. Kein automatisches Wiederholen bis gruen.

Tools/Test-PublishedRelease.ps1 prueft nach Publikation das exakte oeffentliche
latest-Asset und dessen SHA256 durch einen echten vollstaendigen Menue-
Download im unveraenderten Produktionskernel. Beobachtung ueber SSH und
QMP, unabhaengige FAT-/Dateihashpruefung nach Terminal-POWEROFF sowie
Bytegleichheit aller anderen Partitionen. Dieser Kanalnachweis wird erst
mit Recovery in 0.76.25 und dem finalen R4OS in 0.76.26 ausgefuehrt.
Keine Windows-/Lenovo-Hardwareabnahme in dieser automatisierten Stufe.
