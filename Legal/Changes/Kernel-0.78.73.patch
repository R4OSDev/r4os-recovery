diff --git a/Kernel/Support/ntfs_format.zig b/Kernel/Support/ntfs_format.zig
index 58217af..2b004f1 100644
--- a/Kernel/Support/ntfs_format.zig
+++ b/Kernel/Support/ntfs_format.zig
@@ -24,6 +24,10 @@ pub const END_MARKER: u32 = 0xFFFFFFFF;
 pub const UPCASE_ENTRIES: usize = 65536;
 pub const UPCASE_BYTES: usize = UPCASE_ENTRIES * 2;
 
+/// R4OS volume API policy, including the final file component; not an
+/// on-disk NTFS format limit. Builders and verifiers share this boundary.
+pub const R4OS_PATH_SEGMENTS_MAX: usize = 24;
+
 // Well-known MFT record numbers.
 pub const MFT_RECORD_MFT: u64 = 0;
 pub const MFT_RECORD_MFTMIRR: u64 = 1;
diff --git a/Kernel/Support/ntfs_volume.zig b/Kernel/Support/ntfs_volume.zig
index 8740979..468e419 100644
--- a/Kernel/Support/ntfs_volume.zig
+++ b/Kernel/Support/ntfs_volume.zig
@@ -48,7 +48,7 @@ pub const METADATA_PATH_CAPACITY: usize = 8;
 pub const METADATA_CACHE_SLOT_CAPACITY: usize = METADATA_RECORD_CAPACITY +
     METADATA_ATTRIBUTE_CAPACITY + METADATA_INDEX_CAPACITY + METADATA_PATH_CAPACITY;
 const MAX_INDEX_DEPTH: usize = 8;
-const MAX_PATH_DEPTH: usize = 24;
+const MAX_PATH_DEPTH: usize = ntfs.R4OS_PATH_SEGMENTS_MAX;
 const MAX_DATA_RUNS: usize = 24;
 const U32_MAX: u64 = 0xffff_ffff;
 
